# دليل رفع ملف SQL إلى phpMyAdmin

## الملف الجاهز للرفع

**الملف:** `backups\timepay_unified_clean_2026-01-18_05-05-55.sql`
**الحجم:** 0.17 MB
**الترميز:** UTF-8

## خطوات الرفع إلى phpMyAdmin

### 1. الوصول إلى phpMyAdmin على سيرفر الإنتاج
- افتح: `https://timepay.borgelarabpress.com/phpmyadmin` (أو رابط phpMyAdmin الخاص بك)
- سجل الدخول

### 2. اختيار قاعدة البيانات
- من القائمة الجانبية، اختر قاعدة البيانات: `u362313043_timepayDB`
- أو أنشئ قاعدة بيانات جديدة إذا لم تكن موجودة

### 3. استيراد الملف
- اضغط على تبويب **"Import"** (استيراد) في الأعلى
- اضغط على **"Choose File"** (اختر ملف)
- اختر الملف: `timepay_unified_clean_2026-01-18_05-05-55.sql`
- في قسم **"Character set of the file"**: اختر **"utf8"** أو **"utf8mb4"**
- تأكد من أن **"SQL compatibility mode"** = **"NONE"**
- اضغط على **"Go"** (تنفيذ)

### 4. انتظار اكتمال الاستيراد
- قد يستغرق الاستيراد بضع دقائق حسب حجم الملف
- لا تغلق الصفحة حتى يكتمل الاستيراد

## ملاحظات مهمة

### إذا ظهرت النصوص العربية بشكل خاطئ بعد الاستيراد:

1. **تحقق من ترميز قاعدة البيانات:**
   - في phpMyAdmin، اختر قاعدة البيانات
   - اضغط على "Operations"
   - تأكد من أن "Collation" = `utf8mb4_unicode_ci`

2. **تحقق من ترميز الجداول:**
   - افتح كل جدول
   - اضغط على "Operations"
   - تأكد من أن "Collation = `utf8mb4_unicode_ci`

3. **إذا استمرت المشكلة:**
   - قد تكون البيانات نفسها مخزنة بترميز خاطئ في قاعدة البيانات الأصلية
   - ستحتاج إلى إعادة إدخال البيانات يدوياً أو تصديرها من مصدر آخر

## معلومات قاعدة البيانات للإنتاج

- **Host:** localhost
- **Database:** u362313043_timepayDB
- **Username:** u362313043_timepay
- **Password:** BR5E;Sa*8|Oe
- **Charset:** utf8mb4
- **Collation:** utf8mb4_unicode_ci

## استكشاف الأخطاء

### خطأ: "File too large"
- قم بتقسيم الملف إلى أجزاء أصغر
- أو استخدم سطر الأوامر: `mysql -u user -p database < file.sql`

### خطأ: "Syntax error"
- تأكد من أن الملف لم يتلف أثناء الرفع
- جرب رفع الملف مرة أخرى

### خطأ: "Access denied"
- تحقق من صلاحيات المستخدم
- تأكد من أن المستخدم لديه صلاحيات CREATE و INSERT

## بديل: استخدام سطر الأوامر

إذا واجهت مشاكل مع phpMyAdmin، استخدم سطر الأوامر:

```bash
mysql -u u362313043_timepay -p u362313043_timepayDB < timepay_unified_clean_2026-01-18_05-05-55.sql
```

سيطلب منك إدخال كلمة المرور: `BR5E;Sa*8|Oe`

## الملفات المتاحة

- `timepay_unified_clean_2026-01-18_05-05-55.sql` - الملف النهائي للرفع
- `backup_before_fix_2026-01-18_05-05-55.sql` - نسخة احتياطية قبل الإصلاح
