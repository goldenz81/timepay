# إصلاح مشاكل جدول weekly_wage_entitlements

## المشاكل المكتشفة:

### 1. **أعمدة غير مرغوب فيها** (تنتمي للجدول الرئيسي `net_weekly_wage`):
   - `employee_code` (id: 52) - الكود
   - `name` (id: 53) - الاسم
   - `total_deductions` (id: 56) - إجمالي المستقطعات (يجب أن يكون في جدول المستقطعات فقط)
   - `net_salary` (id: 57) - صافي المرتب (يجب أن يكون في الجدول الرئيسي فقط)
   - `cost_center` (id: 58) - مركز التكلفة (يجب أن يكون في الجدول الرئيسي فقط)

### 2. **أنواع بيانات خاطئة**:
   - `total_entitlements` (id: 51): نوع `text` → يجب أن يكون `currency`
   - `discrimination_incentive_allowance` (id: 40): نوع `text` → يجب أن يكون `currency`

### 3. **إعدادات خاطئة**:
   - `total_entitlements` (id: 51): `is_calculated = 0` → يجب أن يكون `1` (لأنه محسوب)
   - `total_entitlements` (id: 51): `is_currency = 0` → يجب أن يكون `1`

## الحل:

تم إنشاء ملف SQL (`fix_weekly_wage_entitlements.sql`) لإصلاح جميع المشاكل:

1. **حذف الأعمدة غير المرغوب فيها**
2. **إصلاح نوع البيانات** لـ `total_entitlements` و `discrimination_incentive_allowance`
3. **إصلاح الإعدادات** لضمان ظهور الأعمدة بشكل صحيح

## كيفية التنفيذ:

1. افتح phpMyAdmin أو أي أداة إدارة قاعدة البيانات
2. اختر قاعدة البيانات `timepay_unified`
3. افتح تبويب SQL
4. انسخ محتوى ملف `fix_weekly_wage_entitlements.sql`
5. الصق المحتوى في حقل SQL
6. اضغط "تنفيذ"

## النتيجة المتوقعة:

بعد التنفيذ:
- ✅ سيتم حذف الأعمدة غير المرغوب فيها
- ✅ سيتم إصلاح أنواع البيانات
- ✅ سيتم إصلاح الإعدادات
- ✅ ستظهر الأعمدة بشكل صحيح في إدارة الأعمدة
- ✅ ستظهر الأعمدة بشكل صحيح في عرض تفاصيل الأجر الأسبوعي

## ملاحظات:

- **لا تقم بحذف الأعمدة يدوياً** - استخدم السكريبت فقط
- **احتفظ بنسخة احتياطية** قبل التنفيذ
- بعد التنفيذ، **أعد تحميل الصفحة** في إدارة الأعمدة

